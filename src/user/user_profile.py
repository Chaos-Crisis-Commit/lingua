"""Initialize user profile in DB"""


class User:
    def __init__(self) -> None:
        """User's:
        userID
        name
        target language(s)
        native language(s)
        preferred language: (english for now)
        level

        if input provided, construct user object by grabbing from DB
        """

    def edit_user(self):
        """edits user's info in DB currently"""
